interface:
  display_name: "Truing"
  short_description: "Repo-specific patterns and workflows for truing"
  default_prompt: "Use the truing repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true